<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.app.myacl" >
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.mobile.app.myacl.Splash"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.mobile.app.myacl.ProfileCreate"
            android:label="@string/app_name" ></activity>
        <activity
            android:name="com.mobile.app.myacl.ProfileShow"
            android:label="@string/app_name" ></activity>
        <activity
            android:name="com.mobile.app.myacl.Home"
            android:label="@string/app_name" ></activity>
        <activity
            android:name=".exerciseShow.ExerciseTabs"
            android:label="@string/app_name" ></activity>
    </application>

</manifest>
